import { stringyJson } from 'gamecartcart/helpers/stringy-json';
import { module, test } from 'qunit';

module('Unit | Helper | stringy json');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringyJson([42]);
  assert.ok(result);
});
